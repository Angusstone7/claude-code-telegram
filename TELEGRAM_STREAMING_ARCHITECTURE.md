# Механизм стриминга текста от ИИ в Telegram

## 📋 Обзор

Документ описывает архитектуру системы стриминга текста от Claude (или другого ИИ) в Telegram с отображением событий, размышлений, результатов, ошибок и статусов операций.

---

## 🏗️ Архитектура системы

### Основные компоненты

```
┌─────────────────────────────────────────────────────────────────┐
│                        ИИ Ответ (SSE Stream)                    │
└──────────────────────┬──────────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────────┐
│                    StreamingHandler                             │
│  • Буферизация текста                                            │
│  • Управление состоянием UI                                      │
│  • Rate limiting через координатор                               │
└──────────────────────┬──────────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────────┐
│              MessageUpdateCoordinator                           │
│  • Централизованная очередь обновлений                          │
│  • Интервал 2 секунды между обновлениями                        │
│  • Объединение множественных запросов                           │
└──────────────────────┬──────────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────────┐
│                  StreamingUIState                               │
│  • Структурированное состояние UI                               │
│  • Рендеринг контента + tools в правильном порядке              │
│  • Компонентная архитектура (без строковых манипуляций)         │
└──────────────────────┬──────────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────────┐
│                 formatting.py                                   │
│  • Markdown → HTML конвертация                                  │
│  • Placeholder-защита от повторной обработки                    │
│  • Обработка незакрытых тегов (для streaming)                   │
└──────────────────────┬──────────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Telegram API                                 │
│  • Редактирование сообщений                                     │
│  • HTML разметка                                                │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📁 Структура файлов

```
presentation/handlers/
├── streaming/
│   ├── handler.py           # StreamingHandler - основной класс
│   ├── formatting.py        # Markdown → HTML, placeholder-защита
│   ├── trackers.py          # Heartbeat, Progress, FileChange trackers
│   ├── step_handler.py      # StepStreamingHandler - инструментарий
│   └── __init__.py
├── streaming_ui.py          # StreamingUIState - компонентная модель UI
└── state/
    └── update_coordinator.py # MessageUpdateCoordinator - rate limiting
```

---

## 🔑 Ключевые классы

### 1. `StreamingHandler` (streaming/handler.py)

**Назначение**: Основной класс для стриминга текста в Telegram.

**Основные методы**:
- `append(text)` - добавить текст в буфер
- `set_status(status)` - установить статус-строку
- `show_tool_use(name, details)` - показать использование инструмента
- `show_tool_result(output, success)` - показать результат инструмента
- `show_todo_list(todos)` - показать план задач (отдельное сообщение)
- `show_question(id, questions, keyboard)` - показать вопрос от ИИ
- `finalize(text)` - завершить стриминг
- `send_error(error)` - отправить ошибку

**Важные атрибуты**:
- `buffer: str` - буфер текста
- `ui: StreamingUIState` - структурированное состояние UI
- `_status_line: str` - строка статуса (внизу сообщения)
- `_coordinator: MessageUpdateCoordinator` - координатор обновлений

### 2. `StreamingUIState` (streaming_ui.py)

**Назначение**: Компонентная модель UI для стриминга.

**Структура**:
```python
@dataclass
class StreamingUIState:
    elements: List[UIElement]      # CONTENT и TOOL в порядке добавления
    _content_buffer: str            # Буфер для текущего контента
    tools: List[ToolState]          # Список инструментов
    thinking: List[ThinkingBlock]   # Блоки размышлений
    thinking_buffer: str            # Буфер для текущего размышления
    completion_info: str            # Стоимость и токены (внизу)
    completion_status: str          # "✅ Готово" (самый низ)
    finalized: bool                 # Флаг завершения
```

**UIElement**:
```python
@dataclass
class UIElement:
    type: ElementType       # CONTENT или TOOL
    data: Union[str, ToolState]
    collapsed: bool         # Свернуть в expandable blockquote
```

**ToolState**:
```python
@dataclass
class ToolState:
    id: str                  # "tool_0", "tool_1", ...
    name: str                # "bash", "read", "write", ...
    status: ToolStatus       # PENDING, EXECUTING, COMPLETED, ERROR
    detail: str              # Краткая деталь (имя файла, команда)
    output: str              # Вывод для блока кода
    change_info: str         # "+5 -3 lines"
```

**Основные методы**:
- `append_content(text)` - добавить текст в буфер
- `add_tool(name, detail, status)` - добавить инструмент
- `complete_tool(name, success, ...)` - завершить инструмент
- `add_thinking(text)` - добавить размышление
- `render()` - рендерить весь state в HTML

### 3. `MessageUpdateCoordinator` (state/update_coordinator.py)

**Назначение**: Централизованный rate limiting для обновлений сообщений.

**Ключевые константы**:
```python
MIN_UPDATE_INTERVAL = 2.0  # секунды между обновлениями
MAX_RATE_LIMIT_WAIT = 10.0 # макс. время ожидания rate limit
```

**Основные методы**:
- `update(message, text, ...)` - обновить сообщение (с debouncing)
- `send_new(chat_id, text, ...)` - отправить новое сообщение

**Логика работы**:
1. Проверка интервала (минимум 2 секунды)
2. Если можно обновить → выполнить сразу
3. Если нет → запланировать отложенное обновление
4. Множественные запросы объединяются (последний побеждает)

### 4. `StableHTMLFormatter` / `IncrementalFormatter` (streaming/formatting.py)

**Назначение**: Конвертация Markdown в HTML с защитой от мерцания.

**Основные функции**:
- `markdown_to_html(text, is_streaming)` - конвертация Markdown → HTML
- `prepare_html_for_telegram(text, is_final)` - закрыть незакрытые теги
- `get_open_html_tags(text)` - найти незакрытые теги

**Placeholder-защита**:
Использует Unicode Private Use Area (U+E000-U+F8FF) для защиты уже обработанных блоков от повторной конвертации.

**Незакрытые конструкции** (для streaming):
- Незакрытые кодовые блоки (``` без закрывающего)
- Незакрытые блокquote
- Незакрытое форматирование в конце текста

### 5. `HeartbeatTracker` (streaming/trackers.py)

**Назначение**: Периодические обновления статуса со спиннером.

**Основные параметры**:
```python
SPINNERS = ["⠋", "⠙", "⠹", "⠸", "⠼", "⠴", "⠦", "⠧", "⠇", "⠏"]
DEFAULT_INTERVAL = 2.0  # синхронизировано с координатором

ACTION_EMOJIS = {
    "thinking": "🧠",
    "reading": "📖",
    "writing": "✍️",
    "editing": "✏️",
    "searching": "🔎",
    ...
}
```

**Методы**:
- `set_action(action, detail)` - установить текущее действие
- `start()` - запустить периодические обновления
- `stop()` - остановить

### 6. `StepStreamingHandler` (streaming/step_handler.py)

**Назначение**: Обёртка для отображения шагов инструментов.

**Методы**:
- `on_permission_request(tool_name, tool_input)` - показать PENDING статус
- `on_permission_granted(tool_name)` - перевести в EXECUTING
- `on_tool_start(tool_name, tool_input)` - начать выполнение
- `on_tool_complete(tool_name, ...)` - завершить выполнение
- `on_thinking(text)` - добавить размышление

---

## 🔄 Жизненный цикл стриминга

### 1. Инициализация

```python
from presentation.handlers.streaming import StreamingHandler

streaming = StreamingHandler(
    bot=bot,
    chat_id=chat_id,
    initial_message=None,
    reply_markup=cancel_keyboard,
    coordinator=coordinator  # опционально
)

await streaming.start("🤖 Запускаю...")
```

### 2. Добавление контента

```python
# Просто текст
await streaming.append("Привет! Я анализирую код...\n")

# Использование инструмента
await streaming.show_tool_use("read", details="main.py")
# ... инструмент выполняется ...
await streaming.show_tool_result("Содержимое файла...", success=True)
```

### 3. Обновление статуса

```python
# Через HeartbeatTracker
heartbeat = HeartbeatTracker(streaming)
await heartbeat.start()
heartbeat.set_action("reading", detail="main.py")
# ... периодически обновляется автоматически ...

# Или напрямую
await streaming.set_status("🧠 Думаю... ⠋ (5с)")
```

### 4. Показ размышлений

```python
# Через UI state напрямую
streaming.ui.add_thinking("Анализирую архитектуру...")
await streaming._do_update()

# Или через StepStreamingHandler
step_handler = StepStreamingHandler(streaming)
await step_handler.on_thinking("Изучаю структуру проекта...")
```

### 5. Показ инструментов

```python
# Способ 1: через базовый API
await streaming.show_tool_use("bash", "npm install")
await streaming.show_tool_result("Installed 123 packages...", success=True)

# Способ 2: через StepStreamingHandler (рекомендуется)
await step_handler.on_tool_start("bash", {"command": "npm install"})
# ... выполнение ...
await step_handler.on_tool_complete("bash", success=True)
```

### 6. Завершение

```python
# Установить информацию о завершении
streaming.set_completion_info("$0.1234 | ~10K токенов")

# Завершить стриминг
await streaming.send_completion(success=True)

# Или с ошибкой
await streaming.send_error("Ошибка подключения к API")
```

---

## 🎨 Отображение различных типов контента

### Текстовый контент

```python
# Markdown поддерживается
await streaming.append("**Жирный** и *курсив* текст\n")
await streaming.append("`код` в строке\n")
await streaming.append("```\nБлок кода\n```\n")
```

### Статус операций

```python
# Внизу сообщения (всегда виден)
await streaming.set_status("🧠 <b>Думаю</b> ⠋ (10с) · <i>main.py</i>")
```

### Инструменты

```python
# Через StreamingUIState
streaming.ui.add_tool("read", "main.py", ToolStatus.EXECUTING)
# ... later ...
streaming.ui.complete_tool("read", success=True)
```

**Отображение**:
- ⏳ Ожидаю разрешение: `read`
- 🔧 Читаю... `main.py`...
- ✅ Выполнено `main.py` (+5 -3 lines)

### Размышления (thinking)

```python
# В свернутом блоке
streaming.ui.add_thinking("Анализирую код...")
```

**Отображение**:
- 💭 <i>Анализирую код...</i> (развернуто)
- <blockquote expandable>💭 Анализирую код...</blockquote> (свернуто)

### План задач (todo list)

```python
todos = [
    {"content": "Анализ кода", "status": "completed", "activeForm": "Анализирую код"},
    {"content": "Рефакторинг", "status": "in_progress", "activeForm": "Рефакторинг кода"},
    {"content": "Тесты", "status": "pending", "activeForm": "Написание тестов"},
]

await streaming.show_todo_list(todos)
```

**Отображение** (отдельное сообщение):
```
📋 План выполнения:

  ✅ ~~Анализирую код~~
  ⏳ Рефакторинг кода
  ⬜ Написание тестов

Прогресс: 1/3
```

### Вопрос от ИИ

```python
questions = [{
    "header": "Выбор подхода",
    "question": "Какой паттерн использовать?",
    "options": [
        {"label": "Singleton", "description": "Один экземпляр"},
        {"label": "Factory", "description": "Фабрика методов"}
    ]
}]

keyboard = Keyboards.question_options(questions, question_id="q1")
await streaming.show_question("q1", questions, keyboard)
```

---

## 🛡️ Rate Limiting и ошибки

### Rate limiting

**MessageUpdateCoordinator** обеспечивает:
- Минимум 2 секунды между обновлениями
- Объединение множественных запросов
- Graceful обработку `TelegramRetryAfter`

```python
# Обновление автоматически задержится на 2 секунды
await streaming.append("Текст")
await streaming.append("Ещё текст")  # Объединится с предыдущим
```

### Ошибки

1. **Сообщение не изменяется** - игнорируется (норма)
2. **Сообщение удалено** - очищается из координатора
3. **Rate limit** - ожидание и повтор
4. **Ошибка парсинга HTML** - fallback на plain text

---

## 📦 Примеры использования

### Пример 1: Простой стриминг

```python
async def stream_simple_response(bot, chat_id, text_stream):
    streaming = StreamingHandler(bot, chat_id)
    await streaming.start()

    async for chunk in text_stream:
        await streaming.append(chunk)

    await streaming.finalize()
```

### Пример 2: С инструментами

```python
async def stream_with_tools(bot, chat_id):
    streaming = StreamingHandler(bot, chat_id)
    step = StepStreamingHandler(streaming)
    heartbeat = HeartbeatTracker(streaming)

    await streaming.start()
    await heartbeat.start()

    # Чтение файла
    heartbeat.set_action("reading", "main.py")
    await step.on_tool_start("read", {"file_path": "main.py"})
    content = await read_file("main.py")
    await step.on_tool_complete("read", success=True)

    # Анализ
    heartbeat.set_action("thinking")
    await step.on_thinking("Анализирую структуру...")

    # Запись результата
    heartbeat.set_action("writing", "result.txt")
    await step.on_tool_start("write", {"file_path": "result.txt"})
    await write_file("result.txt", "...")
    await step.on_tool_complete("write", success=True)

    await heartbeat.stop()
    await streaming.send_completion(success=True)
```

### Пример 3: С вопросом к пользователю

```python
async def stream_with_question(bot, chat_id):
    streaming = StreamingHandler(bot, chat_id)
    await streaming.start()

    await streaming.append("Нужно выбрать подход...\n")

    questions = [{
        "question": "Какую базу данных использовать?",
        "options": [
            {"label": "PostgreSQL", "description": "Реляционная БД"},
            {"label": "MongoDB", "description": "Документная БД"}
        ]
    }]

    keyboard = Keyboards.question_options(questions, "db_choice")
    msg = await streaming.show_question("db_choice", questions, keyboard)

    # Ждём ответа через callback handler...
```

---

## 🔧 Интеграция с другим проектом

### Шаг 1: Скопировать файлы

```bash
# Скопировать директорию streaming/
cp -r presentation/handlers/streaming/ your_project/handlers/

# Скопировать streaming_ui.py
cp presentation/handlers/streaming_ui.py your_project/handlers/

# Скопировать координатор
cp presentation/handlers/state/update_coordinator.py your_project/state/
```

### Шаг 2: Инициализировать координатор

```python
# В main.py или при старте бота
from presentation.handlers.state.update_coordinator import init_coordinator
from aiogram import Bot

bot = Bot(token="...")
coordinator = init_coordinator(bot)
```

### Шаг 3: Использовать в обработчиках

```python
from presentation.handlers.streaming import StreamingHandler
from presentation.handlers.streaming_ui import ToolStatus

async def handle_ai_response(bot, chat_id, response_stream):
    streaming = StreamingHandler(bot, chat_id, coordinator=coordinator)
    await streaming.start("🤖 Думаю...")

    async for event in response_stream:
        if event.type == "content":
            await streaming.append(event.text)
        elif event.type == "tool_start":
            streaming.ui.add_tool(event.tool_name, event.detail, ToolStatus.EXECUTING)
            await streaming._do_update()
        elif event.type == "tool_complete":
            streaming.ui.complete_tool(event.tool_name, event.success)
            await streaming._do_update()

    await streaming.send_completion(success=True)
```

### Шаг 4: Обработчики callback для вопросов

```python
@router.callback_query(QuestionCallback.filter())
async def handle_question_answer(callback: CallbackQuery, callback_data: dict):
    question_id = callback_data["question_id"]
    answer = callback_data["answer"]

    # Отправить ответ обратно в ИИ...
    # Обновить или удалить сообщение с вопросом
    await callback.message.delete()
```

---

## 🎯 Ключевые принципы

### 1. Rate Limiting
- **Всегда** использовать `MessageUpdateCoordinator` для обновлений
- Минимум 2 секунды между редактированиями одного сообщения
- Heartbeat также с интервалом 2 секунды

### 2. Структурированное состояние
- Использовать `StreamingUIState` вместо строковых манипуляций
- `add_tool()` автоматически flush-ит контент перед добавлением
- `render()` всегда возвращает корректный HTML

### 3. Placeholder-защита
- Защищает уже обработанные блоки от повторной конвертации
- Использует Unicode Private Use Area (U+E000-U+F8FF)
- Критично для предотвращения мерцания

### 4. Незакрытые конструкции
- `is_streaming=True` позволяет обрабатывать незакрытые ```, `<blockquote>`
- `prepare_html_for_telegram()` закрывает теги перед отправкой
- Это позволяет видеть промежуточное состояние при стриминге

### 5. Мульти-сообщения
- При переполнении (>4000 символов) создается новое сообщение
- Старое сообщение финализируется (убираются статус и кнопки)
- Новое сообщение начинается с "📨 Часть N"

---

## 📝 Резюме

**Архитектура обеспечивает**:
- ✅ Плавный стриминг текста без мерцания
- ✅ Правильное отображение инструментов и статусов
- ✅ Rate limiting без превышения лимитов Telegram
- ✅ Компонентную модель UI (без строковых хаков)
- ✅ Обработку ошибок и graceful degradation
- ✅ Масштабируемость (добавление новых типов контента)

**Основные файлы для интеграции**:
1. `streaming/handler.py` - StreamingHandler
2. `streaming_ui.py` - StreamingUIState
3. `streaming/formatting.py` - Markdown → HTML
4. `state/update_coordinator.py` - Rate limiting
5. `streaming/step_handler.py` - Отображение инструментов
6. `streaming/trackers.py` - Heartbeat, FileChange

---

*Создано на основе анализа кода проекта ubuntu_claude*
